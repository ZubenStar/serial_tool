# 更新检查功能使用指南

## 功能概述

应用程序现在支持自动检查更新功能，可以从 GitHub Releases 或自定义服务器获取最新版本信息。

## 功能特性

- ✅ **自动版本比较**：智能比较语义化版本号
- ✅ **GitHub 集成**：支持从 GitHub Releases 获取更新
- ✅ **自定义服务器**：支持配置自己的更新服务器
- ✅ **后台检查**：异步检查，不阻塞 UI
- ✅ **详细信息**：显示版本号、更新内容、下载链接
- ✅ **一键跳转**：点击可直接打开下载页面

## 使用方法

### 1. 通过工具栏检查更新

在主界面的"高级工具"区域，点击 **"🔄 检查更新"** 按钮。

### 2. 通过版本号检查更新

点击底部状态栏右侧的版本号（如 `v3.1.0`），也可以触发更新检查。

### 3. 更新检查结果

- **有新版本**：弹窗显示版本信息和更新内容，可选择是否立即访问下载页面
- **已是最新版**：提示当前已是最新版本
- **检查失败**：提示网络连接问题或服务器无响应

## 配置指南

### 方式一：使用 GitHub Releases（推荐）

1. **修改配置**

   打开 [`gui_app.py`](gui_app.py:576)，找到以下代码：

   ```python
   self.update_checker = UpdateChecker(
       owner="yourusername",  # 修改为你的GitHub用户名
       repo="serial_tool"      # 修改为你的仓库名
   )
   ```

2. **发布版本**

   在 GitHub 仓库中创建 Release：
   - Tag 格式：`v3.1.0`（必须以 v 开头）
   - Release 标题：可选
   - Release 描述：将作为更新内容显示
   - 附件：上传可执行文件（如 `serial_tool.exe`）

3. **版本号规范**

   [`VERSION`](VERSION:1) 文件格式：
   ```
   3.1.0
   2025-11-11 10:00:00
   ```
   - 第一行：版本号（使用语义化版本号）
   - 第二行：编译时间（可选）

### 方式二：使用自定义服务器

1. **修改配置**

   打开 [`update_checker.py`](update_checker.py:17)，修改：

   ```python
   CUSTOM_UPDATE_URL = "https://yourdomain.com/api/version"
   ```

2. **服务器响应格式**

   你的服务器需要返回以下 JSON 格式：

   ```json
   {
     "version": "3.2.0",
     "name": "重大更新",
     "description": "- 修复了若干 bug\n- 添加了新功能\n- 性能优化",
     "download_url": "https://yourdomain.com/downloads/serial_tool_v3.2.0.exe",
     "published_at": "2025-11-11T10:00:00Z",
     "assets": [
       {
         "name": "serial_tool_v3.2.0.exe",
         "download_url": "https://yourdomain.com/downloads/serial_tool_v3.2.0.exe",
         "size": 15728640
       }
     ]
   }
   ```

## 版本号规范

### 语义化版本号（Semantic Versioning）

格式：`主版本号.次版本号.修订号`，如 `3.1.0`

- **主版本号**（Major）：不兼容的 API 修改
- **次版本号**（Minor）：向下兼容的功能性新增
- **修订号**（Patch）：向下兼容的问题修正

### 版本比较规则

- `3.2.0` > `3.1.0`（次版本号更大）
- `3.1.1` > `3.1.0`（修订号更大）
- `4.0.0` > `3.9.9`（主版本号更大）

## 高级配置

### 修改超时时间

在 [`update_checker.py`](update_checker.py:97) 中：

```python
with urllib.request.urlopen(req, timeout=5) as response:  # 5秒超时
```

### 自定义 User-Agent

```python
headers={'User-Agent': 'YourAppName/1.0'}
```

### 禁用更新检查

如果不需要更新检查功能，可以：

1. 移除工具栏的"检查更新"按钮
2. 移除版本号的点击事件
3. 注释掉 UpdateChecker 的初始化代码

## 测试更新功能

### 本地测试

1. **降低当前版本号**

   修改 [`VERSION`](VERSION:1) 文件为较低版本，如：
   ```
   3.0.0
   ```

2. **运行应用并检查更新**

   点击"检查更新"按钮，应该会提示有新版本（如果你的 GitHub Release 版本更高）。

3. **恢复版本号**

   测试完成后，恢复 VERSION 文件的版本号。

### 模拟网络故障

暂时断开网络连接，点击"检查更新"，应该会提示连接失败。

## 常见问题

### Q1: 提示"无法连接到更新服务器"

**可能原因：**
- 网络连接问题
- GitHub 配置错误（用户名或仓库名不正确）
- GitHub 仓库没有 Releases
- 防火墙阻止了连接

### Q2: 检查更新后没有反应

**可能原因：**
- 已经是最新版本
- 版本号格式不正确（应为 `v3.1.0` 格式）
- Release 的 tag 格式不正确

### Q3: 如何创建 Release

1. 访问 GitHub 仓库页面
2. 点击 "Releases" → "Create a new release"
3. 填写 Tag（如 `v3.1.0`）
4. 填写标题和描述
5. 上传可执行文件
6. 点击 "Publish release"

## 示例配置

### 完整示例：GitHub Release

```python
# gui_app.py
self.update_checker = UpdateChecker(
    owner="myusername",
    repo="serial_tool"
)
```

对应的 GitHub 地址：`https://github.com/myusername/serial_tool/releases`

### 完整示例：自定义服务器

```python
# update_checker.py
CUSTOM_UPDATE_URL = "https://api.myapp.com/version/check"
```

服务器实现（Flask 示例）：

```python
from flask import Flask, jsonify

app = Flask(__name__)

@app.route('/version/check')
def check_version():
    return jsonify({
        "version": "3.2.0",
        "name": "Version 3.2.0",
        "description": "Bug fixes and improvements",
        "download_url": "https://myapp.com/downloads/latest",
        "published_at": "2025-11-11T10:00:00Z",
        "assets": [{
            "name": "serial_tool.exe",
            "download_url": "https://myapp.com/downloads/serial_tool.exe",
            "size": 15728640
        }]
    })
```

## 开发注意事项

1. **测试时记得恢复版本号**
2. **发布时确保 VERSION 文件版本正确**
3. **Release 的 tag 必须以 `v` 开头**
4. **更新说明建议使用 Markdown 格式**
5. **定期测试更新功能是否正常工作**

## 更新日志

- **v3.1.0** (2025-11-11): 添加更新检查功能
  - 支持 GitHub Releases
  - 支持自定义服务器
  - 后台异步检查
  - 一键跳转下载页面